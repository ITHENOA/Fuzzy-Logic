classdef Distance
    properties
    end
    methods (Static)
        function dist = Euclidian(setPoint1,setPoint2)
            % a,b (#points,dimension)
            dist = sqrt(sum((setPoint1-setPoint2).^2, 2));
        end
    end
end